apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insight-viewer.fullname" . }}-test-connection"
  labels:
    {{- include "insight-viewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insight-viewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
