apiVersion: v1
kind: Service
metadata:
  name: { { include "insight-viewer.fullname" . } }
  labels: { { - include "insight-viewer.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "insight-viewer.selectorLabels" . | nindent 4 } }
