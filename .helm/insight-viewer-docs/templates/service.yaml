apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-example.fullname" . }}
  labels:
    {{- include "drone-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drone-example.selectorLabels" . | nindent 4 }}
