branches:
  only:
    - master
    - develop
    - /^(feature|release)\/.*$/
os: linux
language: node_js
node_js: '10'
cache: npm
before_install:
  - npm i -g npm@latest
  - export PATH="$HOME/node_modules/.bin:$PATH"
script:
  - npm run lint
  - npm run test
  - npm run package:build
  - npm run storybook:build
  - npm run ui-guideline:build
deploy:
  # npm publish
  - provider: script
    script: npm run package:publish:ci
    skip_cleanup: true
    on:
      branch: master
  # https://lunit-io.github.io/frontend-components
  - provider: pages
    skip_cleanup: true
    token: $GITHUB_TOKEN
    keep_history: true
    local_dir: ./dist-storybook
    on:
      all_branches: true
  # netlify
  - provider: pages
    skip_cleanup: true
    token: $GITHUB_TOKEN
    keep_history: true
    local_dir: ./dist-storybook
    target_branch: dist/storybook/$TRAVIS_BRANCH
    on:
      all_branches: true
      condition: $TRAVIS_BRANCH =~ ^(master|develop|feature|release)
  # https://lunit-io.github.io/ui-guideline
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep_history: true
    local_dir: ./dist/ui-guideline/browser
    repo: lunit-io/ui-guideline
    on:
      all_branches: true
notifications:
  email:
    on_success: never
    on_failure: always