name: Deploy insight-viewer on staging

on:
  push:
    branches:
      - chore/insight-viewer-staging-deploy-github-action
    paths:
      - .github/workflows/insight-viewer-staging.yml
      - packages/insight-viewer/**
      - apps/insight-viewer-docs/**

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      WORKDIR: .

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}

      - name: Install Packages
        run: yarn
        working-directory: ${{ env.WORKDIR }}

      - name: Run tests
        uses: percy/exec-action@v0.3.1
        with:
          custom-command: 'yarn test:insight-viewer'
        env:
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}

      - name: Build
        run: yarn doc
        working-directory: ${{ env.WORKDIR }}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.CDN_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.CDN_SECRET_KEY }}
          aws-region: us-east-1

      - name: Deploy
        env:
          HOST: insight-viewer.s.lunit.io
        run: |
          aws s3 sync \
            ./out/handbook s3://lunit-cdn-default-virginia/${HOST} \
            --delete
        working-directory: ${{ env.WORKDIR }}
