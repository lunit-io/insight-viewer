/**
 * Retrieves the current image dimensions given an enabled element
 *
 * @param image The Cornerstone image.
 * @param rotation Optional. The rotation angle of the image.
 * @return {{width:Number, height:Number}} The Image dimensions
 * @memberof Internal
 */
export default function _default(image: any, rotation?: number): {
    width: number;
    height: number;
};
