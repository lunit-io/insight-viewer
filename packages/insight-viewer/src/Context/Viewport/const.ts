import { Viewport } from './types'

export const ViewportContextDefaultValue: Viewport = {
  scale: 0,
  invert: false,
  hflip: false,
  vflip: false,
  x: 0,
  y: 0,
  windowWidth: 0,
  windowCenter: 0,
}
