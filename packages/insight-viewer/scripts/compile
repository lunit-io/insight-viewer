#!/usr/bin/env bash
echo "┏━━━ 📦 COMPILE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
microbundle src/cjs/*.ts --jsx React.createElement --tsconfig tsconfig.build.json -f cjs --no-sourcemap &&
microbundle src/*.ts --jsx React.createElement --tsconfig tsconfig.build.json -f esm --no-sourcemap
