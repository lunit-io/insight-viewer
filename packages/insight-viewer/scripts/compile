#!/usr/bin/env bash
echo "┏━━━ 🧳 COMPILE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
microbundle src/*.ts --jsx React.createElement --tsconfig tsconfig.build.json -f modern,esm,cjs
