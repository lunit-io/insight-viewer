#!/usr/bin/env bash
echo "┏━━━ 🚀 PUBLISH ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
yarn lerna version --ignore-changes 'apps/osd-viewer-demo/**'
cd dist && yarn lerna publish
