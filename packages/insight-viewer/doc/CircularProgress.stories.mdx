import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import { default as CircularProgress } from '../src/components/CircularProgress'

<Meta
  title="CircularProgress"
  component={CircularProgress}
  decorators={[]}
  argTypes={{
    progress: {
      control: {
        type: 'range',
        min: 0,
        max: 100,
        step: 10,
      },
    },
  }}
/>

# CircularProgress

<Canvas>
  <Story name="CircularProgress" args={{ progress: 50 }}>
    {args => <CircularProgress {...args} />}
  </Story>
</Canvas>
