import { source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `@lunit/use-shortcut`

단축키를 구현하기 위한 React Hook

# API

<Example example={source('@lunit/use-shortcut/index')} api />

# Sample

<Example example={source(require('./Sample'))}>
  <Preview height={120} />
</Example>
