import { source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# Convert Images

`ArrayBuffer` 형태로 가져온 데이터들은 `CornerstoneImage` 형태로 변환해야 한다.

<Example example={source(require('./ConvertToImages/Basic'))}>
  <Preview height={320}/>
</Example>

