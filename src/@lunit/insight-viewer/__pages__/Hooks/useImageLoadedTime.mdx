import { source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `useImageLoadedTime()`

# API

<Example example={source('@lunit/insight-viewer/hooks/useImageLoadedTime')} api />

# Sample

<Example example={source(require('./useImageLoadedTime/Basic'))}>
  <Preview height={100}/>
</Example>
