import { source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `useImageProgress()`

# API

<Example example={source('@lunit/insight-viewer/hooks/useImageProgress')} api />

# Sample

<Example example={source(require('./useImageProgress/Basic'))}>
  <Preview height={100}/>
</Example>
