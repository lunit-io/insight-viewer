import { component, source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<StrokeText>`

명암 차이로 인한 가독성 저하를 방지하기 위한 좀 더 뚜렷한 외곽선을 가진 Text Component.

SVG Text를 사용한다. <https://www.w3schools.com/graphics/svg_text.asp>

<Example example={component('./StrokeText/Basic')}>
  <Preview height={200}/>
</Example>

# `<CornerstoneViewer>`에 Holder를 사용해서 배치

<Example example={component('./StrokeText/WithHolders')}>
  <InsightViewerPreview height={720}/>
</Example>