import { source } from '@handbook/source';
import { Example } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<ArrowedContourViewer>`

`ArrowedContour` 데이터를 화면에 그린다.

# API

<Example example={source('@lunit/insight-viewer/components/ArrowedContourViewer')} api />

# Sample

<Example example={source(require('./ArrowedContourViewer/Basic'))}>
  <InsightViewerPreview height={720}/>
</Example>

# `ArrowedContour extends Contour`

`<ArrowedContourViewer>` 는 `Contour` 를 상속한 `ArrowedContour` 유형을 필요로 한다. 

<Example example={source('@lunit/insight-viewer/types')} api={['Contour', 'ArrowedContour']} />

# Custom Style

<Example example={source(require('./ArrowedContourViewer/CustomStyle'))}>
  <InsightViewerPreview height={720}/>
</Example>