import { source } from '@handbook/source';
import { Example } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<HeatmapViewer>`

AI에서 내려받는 Heatmap 데이터 형식을 화면에 그린다.

# API

<Example example={source('@lunit/insight-viewer/components/HeatmapViewer')} api />

# Sample

<Example example={source(require('./HeatmapViewer/Basic'))}>
  <InsightViewerPreview height={720}/>
</Example>