import { component, source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `<DCMImage>`

Thumbnail 처리와 같이 `*.dcm` 파일을 사용한 간단한 이미지 출력을 위해 만들었지만, `*.dcm` 렌더링 비용이 너무 많이 들어서 사용하지 않고 있는 Component.

<https://www.notion.so/lunit/dcmtk-commands-4c86b5e7380d4538889f30d42aa1b748#1acd47147d6d4bcba0a276f23743efb9> 해당 Link와 같이 dcmtk 명령어를 통해서 jpg thumbnail을 만들어서 사용하는 쪽으로 방향을 바꿨음.

# API

<Example example={source('@lunit/insight-viewer/components/DCMImage')} api />

# Sample

<Example example={component('../Basic/CornerstoneImage/DCMImage')}>
  <Preview height={280}/>
</Example>