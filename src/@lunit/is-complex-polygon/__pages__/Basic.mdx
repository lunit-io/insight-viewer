import { component, source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `@lunit/is-complex-polygon`

Polygon (`[number, number][]`)이 단순 다각형이 아닌지 판단한다.

Annotation Tool, OPT와 같이 사용자에게 Annotation을 입력 받을때    
잘못된 Polygon이 입력되지 않도록 방지하는데 사용한다.

<Example example={source('../index')} />

# Test

<Example example={component('./Sample')}>
  <InsightViewerPreview height={520} />
</Example>

<Example example={source('../__tests__/isComplexPolygon.test')} />