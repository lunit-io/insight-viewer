import { source } from '@handbook/source';
import { Example } from '@handbook/components';

# `@lunit/is-intersection`

Line `ab` 와 `cd`과 교차하는지 검사한다.

<Example example={source('../index')} />

# Test

<Example example={source('../__tests__/isIntersection.test')} />