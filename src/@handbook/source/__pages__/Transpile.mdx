import { component } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# Slice

<Example example={component('./Transpile/filterExports')}>
  <Preview height={600} style={{overflowY: 'auto'}} />
</Example>