import { component, source } from '@handbook/source';
import { Example, Preview } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `@lunit/use-dialog`

React Hook 형태의 Dialog를 만들기 위한 Wrapper.

> Dialog Hook을 만들기 위한 Wrapper, 직접적으로 사용할 수 있는 Component는 아니다.

# API

<Example example={source('@lunit/use-dialog/useDialog')} api />

# `useAlert()`, `useConfirm()`

아래와 같이 `DialogTemplate<ParamType, RetrunType>` 형식의 Template을 만든 다음,   
`useDialog()`를 사용해서 새로운 Dialog Hook을 만들 수 있다.

<Example example={source('@lunit/opt-components/components/useAlert')} />

<Example example={source('@lunit/opt-components/components/useConfirm')} />

만들어진 Dialog Hook은 아래와 같이 사용할 수 있다.

<Example example={component('./Samples/Basic')}>
  <InsightViewerPreview height={100} />
</Example>

# Custom Dialog 예제

<Example example={component('./Samples/FormDialog')}>
  <InsightViewerPreview height={100} />
</Example>
