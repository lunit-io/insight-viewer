import { component } from '@handbook/source';
import { Example, Preview } from '@handbook/components';
import posMapToImageDataPreview from './Samples/posMapToImageDataPreview.png';

# `posMapToImageData()`

`number[][]` 형태의 Posmap Data를 Canvas Drawing에 사용할 수 있는 `ImageData`로 변환한다.

<img src={posMapToImageDataPreview} style={{maxWidth: 400}} />

`<HeatmapViewer>`에서 사용된다.

<Example example={component('./Samples/posMapImageData')}>
  <Preview height={320} />
</Example>