import { component } from '@handbook/source';
import { Example } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<ProgressViewer>`

`CornerstoneImage`가 Loading 중이라는 것을 사용자에게 알리고,    
`CornerstoneImage`가 Loading 되지 않은 상태에서 User Interaction이 발생하지 않게 해준다.

<Example example={component('./ProgressViewer/Basic')}>
  <InsightViewerPreview height={720}/>
</Example>