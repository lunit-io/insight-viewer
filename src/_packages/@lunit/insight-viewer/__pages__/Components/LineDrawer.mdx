import { component, source } from '@handbook/source';
import { Example } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<LineDrawer>`

`<ContourDrawer>`와 비슷하지만, 내부가 채워지는 Polygon이 아니라 Polyline을 그린다.

# API

<Example example={source('@lunit/insight-viewer/components/LineDrawer')} api />

# Sample

<Example example={component('./LineDrawer/Basic')}>
  <InsightViewerPreview height={720}/>
</Example>

# Styling

CSS Variable을 통해서 색상을 변경할 수 있다.

아래 File에서 `--contour-drawer-*`의 이름을 가진 항목들에 영향을 받는다.

<Example example={source('../../theme/style')}/>

<Example example={component('./LineDrawer/CustomStyle')}>
  <InsightViewerPreview height={720}/>
</Example>