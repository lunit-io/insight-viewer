import { component, source } from '@handbook/source';
import { Example } from '@handbook/components';
import { InsightViewerPreview } from '@lunit/handbook';

# `<ContourViewer>`

`Contour` 데이터를 화면에 그린다.

`<T extends Contour>`로 구성된 Generic Data를 사용한다. 

```ts
export interface ContourViewerProps<T extends Contour> extends InsightViewerGuestProps {
  width: number;
  height: number;
  
  /** Contour 데이터를 상속받은 Annotation 데이터 */
  contours: T[];
  
  /** Mouse Over 등 User Interaction에 의해서 Focus 된 Contour */
  focusedContour: T | null;
  
  /** <svg className={}> */
  className?: string;
  
  /** <svg style={}> */
  style?: CSSProperties;
  
  /**
   * Line에 외곽을 그려준다
   * Line을 두 개 그려서 border를 표현하기 때문에,성능에 민감한 상황인 경우 비활성 시킬 수 있다.
   */
  border?: boolean;
}
```

아래 모든 예제들은 이 Data를 사용하게 된다.

<Example example={source('../../__fixtures__/contour')}/>

`Contour` 데이터를 넣어주면 화면에 Annotation을 그린다.

<Example example={component('./ContourViewer/Basic')}>
  <InsightViewerPreview height={720}/>
</Example>

# Styling

CSS Variable을 통해서 색상을 변경할 수 있다.

아래 File에서 `--contour-viewer-*`의 이름을 가진 항목들에 영향을 받는다.

<Example example={source('../../theme/style')}/>

<Example example={component('./ContourViewer/CustomStyle')}>
  <InsightViewerPreview height={720}/>
</Example>

# Styling with `Contour.dataAttrs`

`Contour.dataAttrs` 데이터에 의해서 `<polygon data-some={value}>`이 입력되게 된다.

해당 `data-*` Attribute를 기준으로 색상을 변경할 수 있다.

<Example example={component('./ContourViewer/CategoryStyle')}>
  <InsightViewerPreview height={720}/>
</Example>

# Styling with Data sequence

혹은 배열의 순서에 따라 각각 다른 색상을 줄 수도 있다.

 <Example example={component('./ContourViewer/SequenceStyle')}>
   <InsightViewerPreview height={720}/>
 </Example>