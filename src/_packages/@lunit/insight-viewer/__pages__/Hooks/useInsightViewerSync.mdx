import { Link } from 'react-router-dom';
import { component } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `useInsightViewerSync()`

`<InsightViewer>`와 그 위에 Layer 되는 `<ContourViewer>`, `<ContourDrawer>`와 같은 Component들의 Viewport를 동기화 시키기 위해 사용된다.

<Link to="/insight-viewer/cornerstonerenderdata">CornerstoneRenderData에서 좀 더 자세히 확인할 수 있다.</Link>

# Sample

<Example example={component('../Basic/CornerstoneRenderData/SetData')}>
  <Preview height={520}/>
</Example>