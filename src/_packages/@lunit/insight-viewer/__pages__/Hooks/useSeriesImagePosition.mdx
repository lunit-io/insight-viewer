import { component } from '@handbook/source';
import { Example, Preview } from '@handbook/components';

# `useSeriesImagePosition()`

`CornerstoneSeriesImage`의 현재 이미지 위치를 알아낼 수 있다.

화면에 `CornerstoneSeriesImage`의 이미지 위치 정보를 표시해야 하는 경우 사용할 수 있다.

<Example example={component('../Basic/CornerstoneImage/DCMImageWithSeries')}>
  <Preview height={280}/>
</Example> 

<Example example={component('../Basic/CornerstoneImage/CornerstoneViewerWithSeries')}>
  <Preview height={520}/>
</Example>